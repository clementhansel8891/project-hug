From 710582331223366430b304d5544efc29658a9f9b Mon Sep 17 00:00:00 2001
From: Ubuntu <ubuntu@localhost.localdomain>
Date: Mon, 29 Jun 2026 10:17:57 +0800
Subject: [PATCH] fix: skip reachability check, increase timeout to 15s,
 connect to tnt-3rlhko live data

---
 docker-compose.yml        | 4 ++--
 docker-entrypoint.sh      | 0
 src/api/zenvix-api.ts     | 2 +-
 src/config/runtime-env.ts | 2 +-
 vps-auto-deploy.sh        | 0
 5 files changed, 4 insertions(+), 4 deletions(-)
 mode change 100644 => 100755 docker-entrypoint.sh
 mode change 100644 => 100755 vps-auto-deploy.sh

diff --git a/docker-compose.yml b/docker-compose.yml
index a621ebc..a5c918d 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -10,8 +10,8 @@ services:
     networks:
       - bfs_default
     restart: unless-stopped
-    volumes:
-      - ./.env.production:/app/.env.production:ro
+    env_file:
+      - .env.production
 
 networks:
   bfs_default:
diff --git a/docker-entrypoint.sh b/docker-entrypoint.sh
old mode 100644
new mode 100755
diff --git a/src/api/zenvix-api.ts b/src/api/zenvix-api.ts
index 5ac233f..d950f02 100644
--- a/src/api/zenvix-api.ts
+++ b/src/api/zenvix-api.ts
@@ -30,7 +30,7 @@ const delay = (ms: number) => new Promise((res) => setTimeout(res, ms));
 export async function fetchAllProducts(): Promise<Product[]> {
   if (isZenvixConfigured()) {
     // Check reachability on first call (5s timeout, cached thereafter)
-    const reachable = await checkZenvixReachable();
+    const reachable = true; // Skip reachability check - go direct
     if (!reachable) {
       return MOCK_PRODUCTS;
     }
diff --git a/src/config/runtime-env.ts b/src/config/runtime-env.ts
index be2315e..0f86c83 100644
--- a/src/config/runtime-env.ts
+++ b/src/config/runtime-env.ts
@@ -127,7 +127,7 @@ export async function checkZenvixReachable(): Promise<boolean> {
   }
 
   const controller = new AbortController();
-  const timeoutId = setTimeout(() => controller.abort(), 5000);
+  const timeoutId = setTimeout(() => controller.abort(), 15000);
 
   try {
     const baseUrl = url.replace(/\/$/, '');
diff --git a/vps-auto-deploy.sh b/vps-auto-deploy.sh
old mode 100644
new mode 100755
-- 
2.43.0

